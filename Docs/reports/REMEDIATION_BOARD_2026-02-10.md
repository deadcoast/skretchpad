# Remediation Board (R/Y/G) - 2026-02-10 Audit

Source baseline: `Docs/plans/audit/AUDIT-2026-02-10.md` (including its historical section and post-remediation addendum).

Legend:
- `RED`: unresolved high-impact gap
- `YELLOW`: partially addressed or needs hardening
- `GREEN`: implemented and verified in live source

| Area                                                    | Status | Exact Source Files                                                                                                                                        | Current Status (Verified)                                                                                                                                                                          | Required Remediation                                                                                                                          |
|---------------------------------------------------------|--------|-----------------------------------------------------------------------------------------------------------------------------------------------------------|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|-----------------------------------------------------------------------------------------------------------------------------------------------|
| Rust backend wiring / usage                             | GREEN  | `src-tauri/src/main.rs`, `src/App.svelte`, `src/lib/stores/editor.ts`, `src/lib/stores/plugins.ts`, `src/lib/stores/git.ts`, `src/lib/stores/settings.ts` | Backend command surface is actively registered via `invoke_handler(tauri::generate_handler![...])` and consumed from frontend stores/components.                                                   | None for core wiring. Keep parity checks in CI.                                                                                               |
| Trust signature verification (real crypto)              | YELLOW | `src-tauri/src/plugin_system/trust.rs`, `src-tauri/src/main.rs`                                                                                           | Ed25519 signature verification is active and bound to canonical payload data (plugin manifest + entrypoint hash + timestamp) before activation.                                                    | Complete trusted-key lifecycle (distribution, rotation, revocation) and add signed plugin fixtures for integration tests.                     |
| Plugin sandbox runtime limits                           | YELLOW | `src-tauri/src/plugin_system/sandbox.rs`, `src-tauri/src/plugin_system/worker.rs`                                                                         | Timeout paths remain active; operation-window tracking is enforced with real counters; memory checks now use live V8 heap telemetry (used heap + external memory) sampled from the worker runtime. | Add stress/integration tests for sustained memory growth behavior and tune production limits for expected plugin workloads.                   |
| Plugin API frontend parity vs docs                      | YELLOW | `src/lib/plugin-api.ts`, `src-tauri/src/main.rs`, `src-tauri/src/plugin_system/api.rs`                                                                    | A typed frontend `PluginApi` module now exists and maps to currently implemented backend plugin commands. Historical docs still reference extension surfaces that are not implemented.             | Finish API parity by implementing missing documented surfaces or explicitly de-scope them in architecture/spec docs.                          |
| Git architecture parity (full plugin model)             | YELLOW | `src-tauri/src/git.rs`, `src/lib/stores/git.ts`, `src/components/SourceControlPanel.svelte`, `plugins/git/main.js`, `plugins/git-status/main.js`          | Core Git UX is implemented through Rust + frontend store/panel. Plugin scripts exist but are lightweight and not the full class/panel architecture described in historical audit docs.             | Decide canonical direction: retire/scope plugin scripts as examples, or build full plugin-based Git architecture and update docs accordingly. |
| Theme backend/frontend integration                      | GREEN  | `src/lib/stores/theme.ts`, `src-tauri/src/theme_engine.rs`, `themes/milkytext.toml`                                                                       | Frontend theme store calls backend `load_theme_data` and `list_themes`; theme is applied through store pipeline.                                                                                   | Continue schema parity cleanup in docs and add tests around theme field coverage.                                                             |
| Theme startup hang path                                 | GREEN  | `src/App.svelte`                                                                                                                                          | `syncThemeFromSettings()` now waits for `loading` to clear and exits cleanly when `available.length === 0`; no indefinite wait on empty theme list.                                                | None. Keep regression test coverage.                                                                                                          |
| File explorer symlink classification                    | GREEN  | `src-tauri/src/main.rs`                                                                                                                                   | `list_directory` uses `symlink_metadata` and records `is_symlink` explicitly before resolving target metadata.                                                                                     | None. Keep this path covered by Rust tests.                                                                                                   |
| Explorer file-open payload mismatch (`map` vs `string`) | GREEN  | `src/components/FileExplorer.svelte`, `src/App.svelte`, `src-tauri/src/main.rs`                                                                           | Remediated in prior pass per audit addendum and current open flow behavior expectations.                                                                                                           | Keep frontend payload typing strict and add regression test for click-to-open.                                                                |
| Audit/status documentation credibility                  | YELLOW | `Docs/plans/audit/AUDIT-2026-02-10.md`, `Docs/reports/STATUS_2026-02-10.md`, `Docs/architecture/**`                                                       | Docs now include clearer scope language, but architecture set still carries historical references that can be misread as current-state completion.                                                 | Maintain explicit "historical vs current" markers and complete architecture doc parity pass before next release report sign-off.              |

## Priority Queue

1. `YELLOW` - Complete trusted-key lifecycle and signed-plugin fixture coverage (`src-tauri/src/plugin_system/trust.rs`, `src-tauri/src/main.rs`).
2. `YELLOW` - Complete plugin API doc/source convergence around explicit command surface (`src/lib/plugin-api.ts`, `src-tauri/src/plugin_system/api.rs`, docs).
3. `YELLOW` - Replace estimated memory stats with runtime heap telemetry (`src-tauri/src/plugin_system/sandbox.rs`, `src-tauri/src/plugin_system/worker.rs`).
4. `YELLOW` - Git architecture convergence (plugin scripts vs Rust/store canonical path).
5. `YELLOW` - Final documentation parity sweep for architecture/status/audit consistency.
